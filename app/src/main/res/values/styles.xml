<resources>
    <style name="CircleImageView" parent="Widget.MaterialComponents.ShapeableImageView">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>  <!-- Makes the image perfectly circular -->
    </style>

    <!-- Custom Spinner Theme -->
    <style name="SpinnerTheme" parent="Theme.AppCompat.Light">
        <item name="android:popupBackground">@android:color/white</item>
        <item name="android:textColorPrimary">@color/darkgreen</item>
    </style>

</resources>